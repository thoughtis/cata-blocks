/**
 * The Moments Hero block is rendered entirely server-side (see render.php),
 * so nothing is persisted to post content.
 *
 * @return {null} Nothing to save.
 */
export default function save() {
	return null;
}
